<% @photos.each do |photo| %>
	<%= photo.caption %>
	<% link_to 'show', user_photo_path(current_user, photo) %>
	<% img src = <%=photo.image%> %>
<% end %>

<%= link_to 'new', new_user_photo_path(current_user, photo) %>

